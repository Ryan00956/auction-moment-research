# 返场恢复与重新验证手册

本手册用于“竞拍时刻”未来返场时恢复公开研究项目。返场不等于旧助手自动恢复可用；所有实时能力默认保持研究状态和 `actionable=false`。

## 1. 从冻结快照恢复

1. 解除 GitHub 仓库的 Archived 状态。
2. 从 `season-2026-archive-v1` 创建新分支，不要直接改写冻结标签。
3. 下载该 Release 的 `archive-release-manifest.json` 与 `SHA256SUMS.txt`。
4. 校验所有资产，再安装两个 wheel 和固定的 Windows Python 3.12 依赖。
5. 运行公开数据、研究代码、助手核心和归档清单的全部测试。

冻结环境记录在 [`requirements-runtime-win-py312.txt`](requirements-runtime-win-py312.txt)。该文件记录最后一次已知可安装环境，但不保证未来包索引仍保留所有文件。

在 Windows 上可从归档 Release 下载全部资产后运行 `install_archive.ps1`。脚本会先验证 `SHA256SUMS.txt`，然后创建新的 Python 3.12 环境、解压公开快照并安装两个固定 wheel；它只打印实验性入口，不会自动启动助手或连接设备。若 `py -3.12` 没有注册解释器，可通过 `-PythonExecutable C:\path\to\python.exe` 显式指定 CPython 3.12。

## 2. 先审计规则和界面漂移

至少重新确认：

- 游戏客户端版本、活动名称、档位和五轮结构；
- 公共事件、个人事件、结算方式和报价倍率；
- 模拟器分辨率、缩放、ADB 行为和截图方向；
- 大厅、开局、轮次切换、地图滚动和结算页面的视觉位置；
- 120 项图鉴、品质、尺寸、价值和事件目录是否发生变化；
- 旧数据字段和模型输入在新版本中是否仍具有相同含义。

任何一项发生变化，都应创建新的数据、模型或适配器版本，不能静默覆盖 `public-v1`、`0.2.0b6` 或 beta.6 资产。

## 3. 恢复验证的最低顺序

1. 无设备单元测试和公开发布隐私检查；
2. 静态截图上的 OCR/视觉回归，不读取结算标签；
3. 模拟器只读截图与滚动检查，确认不会发送点击、文本或出价输入；
4. 一整局影子运行：只观察、只记录人工审核结果、不影响游戏；
5. 第二整局重复运行，确认轮次状态不会依赖偶然人工绕过；
6. 新的未消费时间块上注册预测，再在标签到达后评估；
7. 至少两个时间块都通过后，才能讨论是否更新“实验性”状态。

在第 4 步完成前，不得声称实时助手已经跑通一整局。在第 6、7 步完成前，不得将冻结模型描述为经过前瞻验证或可用于生产决策。

## 4. 必须保持的安全边界

- 公开助手不得包含自动出价、点击、键盘输入、封包解析或结算导出；
- 低置信 OCR、证据冲突、地图不完整和未证明的出价前时点必须保持 provisional 或 blocked；
- 赛后结算、最终藏品和未来轮次信息不得进入同轮实时特征；
- 新模型必须使用新版本号、新模型卡、输入哈希、拆分协议和前瞻验证记录；
- 原始截图、视频、PCAP、玩家身份、私有路径和解密资产不得进入公开仓库。

## 5. 重新开放前的发布门槛

只有以下条件全部满足，才应把仓库从休眠状态改为 active：

- 新规则与旧规则的差异已经记录；
- 一整局端到端影子运行至少连续通过两次；
- 新时间块前瞻评估已经完成并公开不确定性；
- Windows 安装、模型下载、重复启动和无设备 fail-closed 已重新验证；
- README、状态文件、数据卡、模型卡和 Release 清单使用同一版本；
- 所有公开资产通过 SHA-256、隐私边界和许可检查。
